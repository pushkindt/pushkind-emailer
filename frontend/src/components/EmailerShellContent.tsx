export function EmailerShellContent() {
  return (
    <main className="foundation-page foundation-shell-page">
      <div className="foundation-card">
        <p className="foundation-eyebrow">pushkind-emailer</p>
        <h1 className="foundation-title">React shell is connected</h1>
        <p className="foundation-copy">
          The shared navbar and user menu are now loaded through a typed shell
          API and can be reused by migrated Emailer pages.
        </p>
      </div>
    </main>
  );
}
