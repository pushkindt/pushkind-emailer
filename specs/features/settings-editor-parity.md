# Settings Editor Parity

## Summary

Make the settings page markdown editor match the index page markdown editor.

## Requirements

- The settings page MUST present the message editor with the same tabbed
  markdown and preview layout used on the index page.
- The settings page MUST continue to render markdown preview from the current
  form value.
- The existing settings save behavior and field validation MUST remain intact.
